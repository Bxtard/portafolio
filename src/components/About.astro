---
import { Image } from 'astro:assets';
import profileImg from '../../public/profile.jpg';
---

<section id="sobre-mi" aria-label="Sobre mí">
  <h2><span class="lang-es">Sobre mí</span><span class="lang-en">About me</span></h2>

  <div class="about glass-card">
    <div class="about__avatar">
      <Image src={profileImg} alt="Foto de Bryan Estrada" class="avatar-img" width={140} height={140} />
    </div>
    <div class="about__text">
      <p class="lang-es">
        Desarrollador <strong>Fullstack</strong> con más de 5 años de experiencia en
        liderazgo de equipos y desarrollo de aplicaciones web. Experto en
        <span class="highlight">React</span>, <span class="highlight">Next.js</span>,
        <span class="highlight">Nest.js</span>, <span class="highlight">Express</span>,
        <span class="highlight">Angular 19</span>, <span class="highlight">.NET 10</span>
        y bases de datos como <span class="highlight">PostgreSQL</span> y
        <span class="highlight">MongoDB</span>.
      </p>
      <p class="lang-es">
        Con sólida gestión de servidores usando <span class="highlight">PM2</span>.
        Comprometido con la entrega de soluciones de software innovadoras y de alta calidad.
      </p>
      <p class="lang-en">
        <strong>Fullstack</strong> Developer with over 5 years of experience in
        team leadership and web application development. Expert in
        <span class="highlight">React</span>, <span class="highlight">Next.js</span>,
        <span class="highlight">Nest.js</span>, <span class="highlight">Express</span>,
        <span class="highlight">Angular 19</span>, <span class="highlight">.NET 10</span>
        and databases such as <span class="highlight">PostgreSQL</span> and
        <span class="highlight">MongoDB</span>.
      </p>
      <p class="lang-en">
        With solid server management using <span class="highlight">PM2</span>.
        Committed to delivering innovative, high-quality software solutions.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    display: flex;
    align-items: center;
    gap: 2.5rem;
    max-width: 960px;
    margin: 0 auto;
  }

  .about__avatar {
    flex-shrink: 0;
  }

  .avatar-img {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid transparent;
    background-image: linear-gradient(var(--bg-primary), var(--bg-primary)),
      linear-gradient(135deg, var(--accent-magenta), var(--accent-purple), var(--accent-cyan));
    background-origin: border-box;
    background-clip: padding-box, border-box;
    box-shadow: 0 0 25px rgba(177, 79, 255, 0.25);
  }

  .about__text p {
    margin-bottom: 1rem;
    line-height: 1.8;
  }

  .about__text p:last-child {
    margin-bottom: 0;
  }

  .highlight {
    color: var(--accent-cyan);
    text-shadow: 0 0 6px rgba(0, 255, 245, 0.3);
  }

  strong {
    color: var(--accent-magenta);
  }

  @media (max-width: 640px) {
    .about {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
